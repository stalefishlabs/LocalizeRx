import XCTest
@testable import LocalizeRx

final class LocalizeRxTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
//        XCTAssertEqual(LocalizeRx().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
